# Changelog

## Unreleased

### Dedicated store and optional Git

- Added optional agent-level `state-flow.json` with `directory` and `autoStart` (default false), read at extension load/reload. Support absolute, HOME and config-relative paths, reject invalid settings before registration/writes, and keep config/source roots independent from storage. SDK profile/storage overrides remain explicit. New-session activation now follows the global flag rather than CWD materialization; resume/tree/stop preserve branch-local mode and never rewrite global settings.
- Completed inline integrated acceptance: 257 tests plus type/import checks, zero context/DAG errors, and a 33-file package dry-run including configuration/storage modules and excluding fixtures. Native Pi checks cover configured automatic startup without Git and stopped/enabled branch restoration after config changes. Advisory structure/header warnings remain; production configuration and Knowledge data were not changed, and no source-repository commit or release was performed.
- Completed explicit Git adoption of current file cohorts and initialization over populated stores. Preserve semantic step, lineage/hot history, exact scope bytes and unrelated staging; retain recoverable file state after publication failure. Git publication now includes selected uncommitted streams instead of treating worktree equality as proof of cold durability. Native Pi adoption/subsequent remote publication and unborn/stale-HEAD/rollback witnesses pass in the 252-test suite with type/import checks.
- Separated default state storage at Pi `agentDir/state-flow` from independent Markdown discovery at `agentDir/knowledge`, with explicit SDK root overrides. Git repositories without remotes persist locally without fake pending-push errors; configured remotes retain exact-commit retry.
- Added Git-absent file persistence and wired it into temporal runtime, strict Pi revision pointers, start/stop, final reconciliation, and restart. Exact root/cohort-bound file references preserve current state and hot history without an unbounded parallel store. Shared worktree exclusion, prepared receipts, and byte-safe CAS/rollback protect both publication paths.
- Distinguished unavailable selected revisions from malformed immutable targets, preventing expired file pointers or transient locks from falling through to an older disabled marker and authorizing replacement state. Native Pi tests verify two immediate barriers, a response transition, and stopped restart without repository commands; enabled legacy Pi state also restores into a missing file store without Git. The integrated file-runtime cohort passes 249 tests plus type/import checks.

### Temporal state correction

- Closed TF-8 and the complete temporal correction after final independent APPROVE of all twenty required properties, diagnostics, native trace preservation, and package evidence. Final validation: 233 tests, TypeScript/import checks, zero context/DAG errors, and a 31-file package inventory dry-run. Advisory structure/header warnings remain; no real repository publication or production Knowledge migration was performed.
- Added selected temporal head, branch-local position, hot-history depth, and per-scope retained-tail diagnostics separately from config/meta. Unavailable runtime now yields unknown counts/freshness and no invented empty materialization. Added a twenty-property acceptance map and indexed package docs; native Pi evidence checks that old tool results remain in the full session trace after exclusion from a later model context.
- Closed TF-7 after independent APPROVE: 43 focused tests, two real Pi flows, and type/import checks verified the 32 scoped offsets, shared boundaries, read-only/error lifecycle, and patch-barrier sibling blocking. That cohort passed 231 tests; temporal diagnostics and final property acceptance followed separately.
- Added enabled-only `read_state` for one lazy effective/global/CWD/session materialization at offset 0–7, with explicit boundary identity and unavailable-history errors. Reads do not publish or advance history; patch barriers block reader siblings. The compact protocol now distinguishes temporal offsets from local patches and memory-only no-patch responses from true semantic no-ops. Tests cover all scoped hot offsets, Git-free reads, lifecycle/abort/defaults, and real Pi barrier/read/error/terminal flow.
- Closed TF-3/TF-6 after independent storage and causality reviews approved the inspected guarantees. Reviewers ran 43 and 110 focused tests, temporary Git recovery/rollback probes, and 200 deterministic sparse/replacement cohorts across retained scoped offsets. This establishes current property evidence, not exhaustive equivalence of every removed pre-cleanup assertion.
- Removed predecessor current-state/journal and Git publication APIs, current-state DTOs, staged explanatory windows, synthetic clocks, and mutable scoped history caches. Legacy writing now exists only in test fixtures; production retains explicit current-snapshot interpretation/migration. Accepted replay cohorts and active-boundary staging replace the old parallel paths, while compact context follows selected-lineage order with existing per-scope projection budgets. Ported distinct safety witnesses to canonical APIs and added equal-state/different-basis, Git-free context, linked-worktree lock, and prepared-receipt regressions.
- Closed TF-5 checkpoint acceptance after independent verification of strict pointer/legacy recovery, pre-runtime origin restart, and the final shared publication guard. The verifier traced every origin-permission assignment and checkpoint emission, reran 12 runtime tests, and checked ordinary and valid-pointer first-command stop/start without finding a directly introduced regression.
- Hardened checkpoint recovery after review: explicit start on a pre-runtime branch establishes an empty session origin without importing a later same-session layer, preserves shared bytes and cold branch history, validates legacy immutable targets before selection, and rejects malformed legacy envelope syntax. Transient-lock retries cover both checkpoint generations; disabled-marker publication requires proven pre-runtime provenance, not merely a missing revision, and unanchored legacy semantics cannot be discarded into that marker. Real Pi tree-navigation regression proves marker restart and recovery of the later branch.
- Replaced newly emitted Pi config/meta snapshots with exact revision pointers or a minimal ordinary-disabled marker. Kept predecessor formats as read-only compatibility input, separated runtime validation from checkpoint serialization, and added strict immutable-target fallback without treating publication-lock failure as a reason to select older state. Mock/real Pi fixtures preserve raw wire evidence and resolve metadata explicitly.
- Closed TF-4 migration acceptance after independent rereview confirmed the three adapter fixes, including cold/remote durability of older-branch recovery. Fixed the remaining stop regression: ordinary disabled stop performs no initialization/publication; after failed branch restoration, stop retries the selected revision and preserves disabled configuration and semantic history on resume. Both paths have regressions.
- Hardened cutover recovery after independent review: install cached runtime state and publication basis atomically, reject unavailable publication, retain selected revision for retry after transient restoration failure, check CWD activation eligibility before global migration, and recover legacy revision semantics without parsing explanatory journals. Added direct regression reproductions; independent verification confirmed all three fixes.
- Switched the live Pi adapter to cached `TemporalRuntime` and canonical checkpoint/tail plus config/meta publication. Fresh `/start` creates the eight owned files without `state.json`; accepted barriers expose the next state immediately and retain the prior state through lazy runtime reads. Real Pi checks cover unchanged shared-stream inheritance and old-branch stop/resume without semantic-file rewinding, using a distinct temporal revision reference.
- Hardened both migration and semantic Git publication after independent review: retain raw original bytes, recheck prepared files, serialize cooperating writers, and require exact prepared-output receipts for commit and conflict-preserving rollback. Git hashes the prepared content rather than later worktree bytes. Interleaving regressions cover migration and ordinary publication, non-UTF-8 rollback, lock contention, and preserved external changes; nonparticipating writers remain outside kernel-atomic transaction guarantees.
- Added explicit conservative storage migration with shared cohort origins, empty replay tails, exact legacy-file removal, isolated-index commits, rollback after caller-index failure, and idempotent retry. It preserves unrelated staged/dirty files and cold Git snapshots, ignores explanatory journal contents, and rejects ambiguous or unsafe input. The adapter now invokes migration and supports revision-linked predecessor restoration; migration review and remediation are accepted.
- Corrected the live acceptance boundary: derive replay patches from complete accepted state after finalized-response reconciliation, preserving trusted artifact freshness and explicitly deleting obsolete replacement metadata. Identical semantic state finalizes the run without a history record, step increment, or semantic commit; the live adapter now consumes the accepted temporal stream.
- Added canonical checkpoint/JSONL-tail writing and strict replay decoding, sharing temporal validation and protected exact-output Git publication. The writer accepts a complete validated temporal view, rejects omitted changed streams and stale/session-mismatched bases, and skips unchanged scopes. Live and cold Git readers recover scope streams without fabricating runtime lineage; historical symlink modes fail closed. Temporary-repository tests preserve every hot state through committed sparse transitions and repeated folding while leaving unrelated files and Git worktree history intact.
- Added strict session config/meta serialization and atomic publication alongside temporal streams, with canonical session identity, explicit lineage, runtime counters, a resolvable self-revision, and durable unconfirmed publication intent. Storage tests prove config-only changes create no semantic step, unrelated Knowledge commits do not rebind the runtime revision, malformed or foreign metadata is rejected, and restart reconciliation can retry the existing accepted commit. The live adapter now uses these files and revision-only Pi checkpoints.
- Replaced the target architecture with temporally indexed materialized state on one active causal lineage, anchored `checkpoint.json` plus bounded replayable tails, separate session config/meta files, lazy historical reads, and lossless current-state migration. Replaced the superseded persistence and Pi integration assumptions with one coherent temporal contract.
- Completed TF-1/TF-2 temporal algebra in `lib/temporal.ts`: explicit parent-linked opaque transition identities, branch-local ordering, immutable per-scope checkpoint folding, shared multi-scope boundaries, and lazy effective/scope reads at offsets zero through seven. True no-ops preserve the head; response-only changes advance it; unproven pre-origin history is unavailable.
- Added temporal tests comparing every available offset against retained snapshots through forty dense and fifty sparse transitions, deletion overlays, shared identities, forks, invalid tails, no-ops, and read boundaries. The final 233-test suite includes canonical safety witnesses, model history reads, temporal diagnostics, and native Pi integration after obsolete-test consolidation; TypeScript and package import checks pass.

### Pre-temporal canonical storage milestone

- Replaced the planned storage contract with repository-root `state.json` and `patches.jsonl`, nested CWD and session scope pairs, Git-backed session state, independent seven-entry journals, and branch-linked revision restoration that never resets the shared worktree.
- Reopened implementation work as SF-17 through SF-22, then completed SF-17's low-level storage migration: canonical global/CWD/session path helpers, collision-safe CWD and Pi session keys, plain scope-owned `state.json` files, bounded same-scope JSONL journals, strict owned-path classification, and atomic state/journal publication.
- Added current-HEAD three-layer runtime initialization using Pi's stable session ID. New sessions create distinct empty session pairs and no longer borrow another same-CWD session's values.
- Completed SF-19's three-scope Git transaction path: session-only, response-only, and multi-scope changes atomically publish affected state/journal pairs in one isolated-index commit, with compare-and-swap conflict detection and exact pending-push retry.
- Completed SF-18 branch-correct loading with object-level reads of all three scope pairs at the checkpoint-linked revision. Historical inspection leaves `HEAD`, index, and worktree untouched; restored publication rebases the complete selected projection only when global/CWD files have not diverged, while normal compare-and-swap catches changes after restoration.
- Completed SF-20 per-scope causality: each journal retains only its scope's latest seven patches, response-only changes create session records, shared cohort identities reassemble across journals without accepting conflicts, and model projection applies its zero-through-seven setting independently to each scope rather than as one combined cap.
- Completed SF-21 lifecycle migration: newly persisted Pi checkpoints contain only runtime config and provenance, legacy checkpoint semantics migrate once into the Git-backed session pair, stop leaves semantic files and Git `HEAD` unchanged, and status reports repository, CWD/session keys, active revision, per-scope counts, and all three Git-backed materializations.
- Completed SF-22 test and architecture reconciliation. Unit and real-Pi coverage now exercise canonical paths, three-scope atomic commits, same-CWD session isolation, independent seven-entry journals, revision-linked tree restoration, worktree-safe historical reads, checkpoint separation, pending-push retry, and response persistence; superseded active assertions were removed.
- Kept Knowledge independent: Markdown discovery begins at the configured repository root, and State Flow reserves no document names, templates, validation protocol, or `save_knowledge` behavior.
- Validated the completed correction with the full 190-test suite, package import/type checks, `git diff --check`, context validation, and npm package inventory dry-run.

### Superseded in-progress implementation record

The entries below describe the uncommitted implementation draft that preceded the correction. They are retained as development history, not as the active storage contract or release-ready behavior.

- Added the canonical four-field `MaterializedState` shared by global, CWD, and session scopes, including deterministic recursive overlay and scope-local deletion semantics.
- Added the path-keyed artifact IR with canonical source hashing, required description/hash/compiler validation, optional typed compilation metadata, forward-compatible unknown metadata, and fail-closed state restoration.
- Added session-initialization discovery for regular `knowledge/**/*.md` sources with canonical path/hash candidates, explicit removal detection, nested-file support, missing-root handling, symlink exclusion, and canonical-root confinement without Knowledge-specific parsing or validation.
- Added deterministic incremental artifact invalidation for new, changed, compiler-revision, malformed-metadata, and explicit-refresh cases, plus immutable batch helpers that atomically apply trusted freshness metadata and removals to registries and materialized scopes.
- Added the compact artifact compiler protocol: ordinary artifacts produce small routing descriptions rather than raw Markdown or whole-file summaries, uncertain contents remain explicitly uncertain, and richer compilations are reserved for reusable operational semantics.
- Generalized source acquisition into one materialized-first policy across artifact kinds: fresh materialized state handles routine and new-session access, while concrete semantic gaps, exact source/edit operations, evidenced invalidation, reconciliation, and explicit requests permit reads; changed hashes always require reacquisition.
- Moved Skill compilations from `contract.compiled_skills` into exact-path local artifacts. Successful reads now require compact compiler output, while the runtime hashes the executed source and owns `skill-artifact-v1` freshness metadata; refreshes replace stale metadata, legacy snapshots migrate without discarding compiled behavior, and new transitions cannot recreate the retired contract store.
- Separated session persistence into runtime-owned `config` and `meta` plus model-maintained semantic `state`, with backward migration for legacy flattened snapshots and terminal guards against patching runtime fields.
- Added independent global and CWD durable materialization under the shared Knowledge Git repository's `.state-flow` namespace. CWD files use Pi-style readable directory keys plus a full canonical-path hash, retain and validate the resolved path to fail closed on collisions, reject malformed/null-bearing/symlinked state files, publish by atomic rename, and classify only explicit State Flow-owned paths without touching unrelated repository contents.
- Added session activation semantics: first start initializes the CWD marker, genuinely new sessions inherit materialized global/CWD state, resume and tree navigation remain branch-local, and stop disables only the current branch while preserving semantic and durable state.
- Added explicit scoped terminal transitions with narrowest-owner guidance, multi-scope staging and compare-and-swap commit, prepared global/CWD publication cohorts, session-only and durable no-op suppression, CWD-owned Skill compilation, strict scope/patch validation, and runtime ownership of finalized session responses. Pre-scoped envelopes remain session-only compatibility shorthand.
- Added a bounded projection of the latest seven materially effective semantic patch cohorts. Runtime identities deduplicate multi-scope transitions, durable envelopes carry global/CWD causality, branch snapshots carry session causality, active-base merging prevents abandoned branch futures from leaking, and new sessions receive recent durable context without old conversation trajectories or historical full states.
- Added Git publication for durable transitions: exact durable-file identities retain compare-and-swap conflict detection while unrelated `HEAD` advances are reconciled as commit ancestry, multi-scope cohorts produce one State Flow-owned commit through an isolated index without capturing Knowledge edits, unrelated staged and dirty work remains intact, and push failures preserve the accepted local commit for exact-commit retry without model regeneration.
- Added opt-in bounded artifact maintenance planning: only otherwise-fresh, old-enough artifacts enter deterministic oldest-first selection; strict read-count and source-byte ceilings defer oversized work; missing timestamps prioritize legacy metadata; correctness invalidation remains primary; and explicit full refresh remains separate for migrations or debugging.
- Expanded `/state-flow-status` into compact operator diagnostics for branch config/mode, CWD identity, snapshot step and durable Git base, scoped artifact and transition counts, inspectable global Markdown staleness reasons, pending push and terminal-retry state, and explicitly labeled durable/session/effective materializations without source-body output.
- Added real Pi SDK integration coverage for ordinary startup, explicit and automatic activation, compaction, tree restoration, branch-local stop, terminal retry, abort, concurrent durable writers, Knowledge-only history advances, Skill execution, restart, and fresh-agent continuation quality.
- Completed ordinary Markdown runtime acquisition: active context projects only stale global path/hash/reason candidates, successful exact-path reads require compact global compiler outputs, and State Flow attaches trusted hash/compiler metadata. Real Pi scenarios prove zero unchanged-source reads, one-source recompilation after a content change, and deterministic compiler-revision invalidation.
- Persisted pending durable-push metadata in the runtime-owned branch snapshot so a resumed process retries the exact accepted local commit without duplicating its semantic transition.
- Published first-time CWD activation as its own State Flow-owned Git commit and push using the same compare-and-swap and pending-publication semantics as later durable transitions.
- Added the sole model tool `patch_state` for immediate `session`, `cwd`, or `global` materialization during long runs. Intermediate barriers reuse the scoped transition, artifact compilation, durable Git publication, pending-push, snapshot, and recent-history pipeline while preserving the prior response and deferring bootstrap/validation finalization to the required terminal reconciliation.
- Enforced barrier semantics against Pi's parallel tool mode: `patch_state` opts the response into sequential preflight, State Flow inspects Pi's synchronized assistant message, blocks every sibling tool before execution, and rematerializes exactly one effective runtime projection for the next inference while retaining current-run tool causality.
- Added branch-local `config.transitionWindow` with a validated zero-through-seven projection range and legacy default migration to seven; retained durable transition windows remain capped independently at seven.
- Materialized deleted Markdown sources as deterministic runtime-owned global transitions without body acquisition or model compiler output.
- Expanded real Pi integration coverage for multiple intermediate barriers across all scopes, next-inference rematerialization, projection replacement, sibling-tool blocking, terminal semantic no-op reconciliation, and deterministic artifact removal.

## 0.3.0: Optional memory patches and evidence-aware handoffs

- Require targeted revalidation of volatile observations and inspection of external effects after interruption or branch navigation; missing memory proves neither success nor absence of effects. Document the absence of rollback/freshness guarantees and preserve the restriction on routine Skill rereads. Add instruction-presence tests, not semantic or integration guarantees.
- Strengthen the handoff protocol with evidence-aware continuation, requirement/observation/hypothesis distinctions, conditional rejections, and contradiction reconciliation; preserve decision-relevant uncertainty during optimization. Add instruction-presence regressions, not semantic guarantees. Increase the protocol-only character budget to 3,700 (3,900 with bootstrap) for these obligations; state remains uncapped.
- Accept ordinary non-empty terminal answers as empty memory patches: preserve memory and update the response without unnecessary regeneration, including answers split across text blocks.
- Keep malformed explicit envelopes and missing Skill compilations on the existing bounded validation path.
- Clear the backlog; documented integration and continuation-quality limitations remain, not release commitments.

## 0.2.3: Document protocol and validation limitations

- Document that duplicate-comment detection also rejects literal State Flow envelope examples inside fenced or inline code in terminal answers, and provide a plain-JSON workaround.
- Clarify that mock lifecycle tests are not real Pi scheduler integration checks and that structural validation cannot guarantee semantic memory retention.
- No runtime behavior changes.

## 0.2.2: README structure hotfix

- Separate installation from usage, remove the redundant quick-start section, and move architecture details after the user-facing documentation.

## 0.2.1: README banner hotfix

- Add the project banner to the README.

## 0.2.0: Modular runtime hardening

- Add a task-first README quick start with explicit compatibility, opt-in behavior, one-off trial instructions, and Pi package security guidance.
- Tighten the normative runtime protocol and enforce a regression-tested character budget; omit absent validation feedback from synthetic runtime context instead of serializing a redundant `null`.
- Redistribute lifecycle coverage from the former monolithic extension suite into the corresponding mirrored domain suites, leaving `extension.test.ts` focused on composition and using one shared test harness.
- Split the runtime into independent `json`, `state`, `episode`, `snapshot`, `session`, `recovery`, `status`, `context`, `skills`, `terminal`, `validation`, `transition`, and `extension` modules under `lib/`; isolate explicit episode transitions, active-branch selection and recovery, status rendering, bounded retry decisions, atomic state staging, and compare-and-swap commits; mirror each domain under `tests/`, enforce an acyclic dependency graph with cross-domain invariant tests, and reduce `index.ts` to composition and public exports.
- Register lifecycle commands in the canonical start, status, stop order.
- Centralize synthetic runtime-context construction and private validation-message filtering in the independently tested context domain, keeping user-controlled specifications out of system-prompt composition.
- Keep State Flow enabled and preserve the last committed state when terminal-validation retries are exhausted; only the transient retry chain is abandoned so the next user request can continue reliably.
- Track Skill reads through an independently tested lifecycle correlator in Pi's actual `tool_execution_start` → `tool_call` event order while retaining the mutable intercepted input reference, so later argument rewrites are attributed to the executed path.
- Discard mismatched lifecycle records defensively so a stale or reused tool-call id cannot produce false Skill acquisition.
- Bound restored iteration and validation-attempt counters before incrementing them, reject exhausted live iteration counters explicitly, and cover both safe-integer boundaries so malformed branch metadata cannot corrupt retry accounting or status output.
- Reject restored non-JSON state values such as non-finite numbers before they can break hashing or silently materialize as `null`.
- Fall back past malformed newer checkpoints to the newest valid active-branch snapshot, while still failing closed on cyclic or hostile snapshot objects when no valid checkpoint remains.
- Reject malformed three-field state shapes rather than reinterpreting them as legacy working memory, and contain failures while enumerating hostile branch entries.
- Make canonical serialization and exported patch validation reject lossy or cyclic non-JSON inputs explicitly instead of returning invalid results or silently rewriting values.

## 0.1.5: Skill attribution and state model clarification

- Attribute successful Skill acquisition to finalized `tool_execution_start` arguments, with a tested compatibility fallback to the intercepted `tool_call` input.
- Clarify that the three-field materialized handoff coexists with mutable exogenous project and runtime state that can also shape model behavior.
- Remove the obsolete `/reload` step from the usage instructions.

## 0.1.4: Release verification hotfix

- Kept npm pack lifecycle output out of release inventory JSON so Trusted Publisher releases can complete public verification and GitHub Release creation.

## 0.1.3: Trusted Publisher release automation

- Added immutable-tag GitHub Actions automation that validates the package, publishes and verifies npm through Trusted Publisher with provenance, and creates the matching GitHub Release.

## 0.1.2: Repository identity hotfix

- Aligned GitHub repository, package metadata, installation documentation, and release links under the canonical `llblab/pi-state-flow` identity.

## 0.1.1: Public npm distribution

- Enabled public npm distribution for `@llblab/pi-state-flow`, including registry access and verified GitHub package metadata.
- Clarified that compiled Skill knowledge is stored persistently under `contract.compiled_skills`, never as another top-level state field.

## 0.1.0

- Added an opt-in State Flow runtime inspired by SKILL.state, with argument-free lifecycle commands, per-user-run specifications, active-branch persistence, and one materialized explicit state.
- Added native Pi tool-loop preservation: tool-bearing responses require no patch, current-run tool trajectory remains model-visible, and one terminal handoff commits state after the complete agent run.
- Added required flexible `contract` and `working` objects plus the required latest user-facing `response` string, recursive materialization, nested object-key `null` deletion, materialized-null rejection, and deterministic state compare-and-swap.
- Kept arbitrary response content outside the transcript-private `<!-- state_flow … -->` memory-patch frame; the runtime strictly anchors the frame at the start of one terminal text block, requires exactly one blank separator, reconciles materialized `response` with Pi's finalized post-handler text at `turn_end`, and routes post-handler finalization failures through the same bounded hidden regeneration chain. Documented that the frame remains observable to streaming consumers before finalization.
- Kept user-controlled turn specifications at user authority by moving their repeated representation out of the system prompt and into synthetic user runtime context, including support for empty text in image-only prompts.
- Added active-branch restoration after `/tree` navigation, clean abandonment of interrupted terminal-validation retries before the next user run, and cumulative retry accounting across intervening tool-bearing turns.
- Preserved persistent and current-run custom context from other Pi extensions, attributed Skill acquisition from finalized executed arguments after mutable tool interception, and limited accidental intermediate-envelope stripping to a structurally valid leading frame.
- Hardened recursive patch materialization so JSON keys such as `__proto__` remain ordinary own data without changing object prototypes.
- Added an explicit release file allowlist, Pi package discovery keywords, package-safe README targets, Pi/Node compatibility bounds, and a prepack validation gate so generated packages contain only runtime and human-facing release files.
- Added bootstrap migration for enabling State Flow inside an existing session, per-run specification rotation, schema-free episode-level Skill compilation in `contract.compiled_skills` keyed by exact source path, and model-owned memory optimization that reorganizes inefficient structure and deletes stale, redundant, speculative, or low-value state.
- Removed per-tool state commits, action authorization, Delta Window, observation envelopes, action ledgers, project schemas, state and patch byte caps, dynamic growth pressure, and all coupling to other extensions.
- Compressed the static runtime protocol while retaining terminal, handoff, Skill-compilation, authority, and memory-optimization invariants.
- Added stop-as-complete-episode-reset semantics, a compact accent `state-flow` plus dim `#<iteration>` status, and complete pretty-formatted state JSON output from `/state-flow-status`.
