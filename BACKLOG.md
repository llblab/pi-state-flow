# Backlog

No open implementation items for the 0.20.0 cache-stable projection scope. Outcomes are recorded in [CHANGELOG.md](CHANGELOG.md); source-bound before/after measurements and their limits are in [performance evidence](docs/performance.md).

Release publication is owned by `.github/workflows/release.yml`. Verify the exact tag's successful workflow, published GitHub Release and matching npm package before reporting release completion. Installed-instance reload remains a separate operator action.

Current contracts belong to [architecture](docs/architecture.md), [lazy state](docs/lazy-state.md), [usage](docs/usage.md) and the bundled Skills. [Temporal acceptance](docs/temporal-acceptance.md) records the native projection/hint witnesses and their limits. Canonical storage, CAS, `historyLimit`, history and lifecycle semantics are unchanged.
